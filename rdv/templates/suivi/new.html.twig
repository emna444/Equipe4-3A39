{% extends 'base.html.twig' %}

{% block title %}Nouveau suivi {% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="card">
            <div class="card-header">
                <h3 class="card-title text-center"><i class="fas fa-plus-circle"></i> Nouveau suivi </h3>
            </div>
            <div class="card-body">
                {{ include('suivi/_form.html.twig') }}
            </div>
            <div class="card-footer">
                <a href="{{ path('app_suivi_index') }}" class="btn btn-primary"><i class="fas fa-arrow-left"></i> Retourner à la listes</a>
            </div>
        </div>
    </div>
{% endblock %}
