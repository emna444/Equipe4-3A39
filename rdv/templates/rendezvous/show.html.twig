{% extends 'base1.html.twig' %}

{% block title %}Détail d'un rendez-vous{% endblock %}

{% block body %}

    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-body">
                        <h1 class="card-title text-center mb-4">Détail d'un rendez-vous</h1>

                        {#<table class="table">
    <tbody>
        <tr>
            <th>Id:</th>
            <td>{{ rendezvou.id }}</td>
            <th>Date:</th>
            <td>{{ rendezvou.date ? rendezvou.date|date('Y-m-d H:i:s') : '' }}</td>
            <th>Etat:</th>
            <td>{{ rendezvou.etat }}</td>
        </tr>
    </tbody>
</table>#}
<div class="card">
    <div class="card-body">
        <h5 class="card-title">details d'un Rendez-vous</h5>
        <dl class="row">
            {#<dt class="col-sm-3">Id:</dt>
            <dd class="col-sm-9">{{ rendezvou.id }}</dd>#}

            <dt class="col-sm-3">Date:</dt>
            <dd class="col-sm-9">{{ rendezvou.date ? rendezvou.date|date('Y-m-d H:i:s') : '' }}</dd>

            <dt class="col-sm-3">Etat:</dt>
            <dd class="col-sm-9">{{ rendezvou.etat }}</dd>

        </dl>
    </div>
</div>
                        <a href="{{ path('app_rendezvous_index') }}" class="btn btn-primary">
                            <i class="fas fa-arrow-left"></i> Retour à la liste
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
