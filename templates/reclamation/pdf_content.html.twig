<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Détails de la réclamation</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: 16px;
            background-color: #f9f9f9;
            margin: 0;
            padding: 0;
        }
        .header {
            background-color: #007bff;
            color: #ffffff;
            padding: 20px;
            text-align: center;
            margin-bottom: 30px;
        }
        .content {
            border: 1px solid #dddddd;
            padding: 20px;
            background-color: #ffffff;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            max-width: 600px;
            margin: 0 auto;
        }
        p {
            margin-bottom: 15px;
        }
        strong {
            color: #007bff;
        }
        .email-form {
            margin-top: 30px;
        }
        .email-input {
            width: 100%;
            padding: 10px;
            font-size: 16px;
            margin-bottom: 10px;
            box-sizing: border-box;
        }
        .send-button {
            background-color: #007bff;
            color: #ffffff;
            padding: 10px 20px;
            border: none;
            cursor: pointer;
            border-radius: 5px;
        }
        .send-button:hover {
            background-color: #0056b3; 
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>Détails de la réclamation</h1>
        <img src="{{ asset('images/logo.png') }}" alt="Logo DEWINI" style="width: 100px; height: auto;">
    </div>
    <div class="content">
        <p><strong>ID :</strong> {{ reclamation.id }}</p>
        <p><strong>Description :</strong> {{ reclamation.description }}</p>
        <p><strong>Date de création :</strong> {{ reclamation.date|date('Y-m-d H:i:s') }}</p>
        <form class="email-form">
            <input type="email" name="email" class="email-input" placeholder="Entrez votre adresse e-mail" required>
            <button type="submit" class="send-button">Envoyer le PDF</button>
        </form>
    </div>
</body>
</html>
